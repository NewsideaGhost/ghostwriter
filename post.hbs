{{!< default}}
<div id="content">
<div class="container">
    {{#post}}
        <article class="{{post_class}}">
            <header class="post-header">
                <h1 class="post-title">{{title}}</h1>

                <p class="post-date"><time datetime="{{published_at}}">Reported {{date format="MMMM Do YYYY"}}</time></p>
            </header>

            <div class="post-content">
                {{{content}}}

                {{#if tags}}
                    <p class="post-tags"><span>Tag:</span> {{tags}}</p>
                {{/if}}
            </div>
        </article>
        {{#if author}}
        <section class="author">
            <img src="{{author.image}}" class="userimage" alt="{{author.name}}" />
            <p class="author-content">Story by: {{author.name}}<br>
            <a href="mailto:{{author.email}}"> {{author.name}}</a><br>
            {{author.bio}}</p>
        </section>
        {{/if}}

    {{/post}}
</div>
{{> footer}}
</div>
